import { StyleSheet } from 'react-native';
import { Colors } from '../../styles';

export default StyleSheet.create({
  buttonTitle: {
    color: Colors.purple,
    textAlign: 'center',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
