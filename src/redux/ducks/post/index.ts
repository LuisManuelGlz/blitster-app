import * as actionTypes from './types';
import * as actions from './actions';
import * as operations from './operations';
import reducer from './reducer';

export default { actionTypes, actions, operations, reducer };
